# Optional: full API base (required if UI and API use different hosts, e.g. production CDN).
# VITE_SERVER_URL=http://localhost:3001
# If unset, the app uses the same hostname as the page + port below (good for LAN testing).
VITE_SERVER_PORT=3001
VITE_CLERK_PUBLISHABLE_KEY=
