/**
 * Optional demo for ShaderBackground — import in a dev route or Storybook if needed.
 */
import ShaderBackground from '@/components/ui/shader-background'

export function ShaderBackgroundDemoOne() {
  return <ShaderBackground />
}
